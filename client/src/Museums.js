import React from "react";
import withNavbar from "./Layout";

function Museums() {
    return (
        <div>
            <h2>Museums</h2>
        </div>
    );
}

export default withNavbar(Museums);
